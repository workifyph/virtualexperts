name: Deploy dev

on:
  push:
    branches: [dev]
  repository_dispatch:
    types: [sanity-publish]
  workflow_dispatch: {}

concurrency:
  group: deploy-dev
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # repository_dispatch lands on the default branch by default; force dev
          ref: dev

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Build (static export)
        env:
          NEXT_PUBLIC_SANITY_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_SANITY_PROJECT_ID }}
          NEXT_PUBLIC_SANITY_DATASET: ${{ secrets.NEXT_PUBLIC_SANITY_DATASET }}
          NEXT_PUBLIC_SANITY_API_VERSION: ${{ secrets.NEXT_PUBLIC_SANITY_API_VERSION }}
          SANITY_API_READ_TOKEN: ${{ secrets.SANITY_API_READ_TOKEN }}
          NEXT_PUBLIC_STUDIO_ENABLED: "true"
        run: npm run build

      - name: Deploy to Cloudflare Pages (dev)
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # head_commit.message can be multiline and contain shell-special chars;
          # pass it via env, then strip to first line at runtime.
          RAW_COMMIT_MSG: ${{ github.event_name == 'repository_dispatch' && 'sanity-publish' || github.event.head_commit.message }}
        run: |
          msg=$(printf '%s' "$RAW_COMMIT_MSG" | head -n1)
          npx wrangler@4 pages deploy out \
            --project-name=virtualexperts-preview \
            --branch=dev \
            --commit-hash=${{ github.sha }} \
            --commit-message="$msg"
